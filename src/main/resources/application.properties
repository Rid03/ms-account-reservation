#spring.application.name=ms-account-reservation
app.name=${APP_NAME:ms-account-reservation}
app.port=${SERVER_PORT:8080}
db.url=${DB_URL:jdbc:postgresql://localhost:5432/account_reservation}
db.username=${DB_USERNAME:postgres}
db.password=${DB_PASSWORD:password}